export default function ExcelMsIcon({ className }: { className?: string }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img
      src="/icons/excel_icon.svg"
      width={20}
      height={20}
      alt=""
      aria-hidden
      className={className}
    />
  );
}
